[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Skip secrets scanning for intentionally public env vars
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_ANON_KEY,NEXT_PUBLIC_SUPABASE_URL,GEMINI_API_KEY"
